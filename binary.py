import csv
import string

# Open the CSV File and read it in.
f = open('countries.csv')
data = f.read()

# Split the data into an array of countries.
countries = data.split('\n')

length = len(countries)
print(countries[1])
print(countries)

"pivot" = (end - start) /2

in range (i=0; i <= n; i++) in (list.length)
if left side
  end = "pivot"
if right side
  start = "pivot"
